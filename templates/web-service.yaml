apiVersion: v1
kind: Service
metadata:
  name: {{ include "growthbook.fullname" . }}-web
  labels:
    {{- include "growthbook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: web
      protocol: TCP
      name: http
  selector:
    {{- include "growthbook.selectorLabels" . | nindent 4 }}
